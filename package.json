{
  "name": "edelweiss",
  "version": "0.0.0",
  "scripts": {
    "start": "docker-compose up -d",
    "build": "tsc",
    "test": "run-s test:*",
    "test:lint": "tslint -c tslint.json 'src/**/*.ts' || true"
  },
  "author": "g-harel",
  "license": "MIT",
  "devDependencies": {
    "npm-run-all": "^4.0.2",
    "tslint": "^5.5.0",
    "typescript": "^2.4.2"
  }
}
