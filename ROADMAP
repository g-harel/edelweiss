1
  domain database
    create
    delete
    read
  user database
    add
    remove
    edit
  api
    domain endpoints
    user endpoints
    auth
  interface
    /home
    /:domain/login
    /:domain/register
    /:domain/view

  ## ? implement queue type mechanism to handle non-read requests ~ workers pulling from queue
  ## ? define some standard interface being returned from db packages

2
  ...